name: anime_tracker
description: Tracker personal de actualizaciones de anime conectado a Firestore.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # Firebase
  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.5

  # Carga de imágenes remotas con caché y placeholders
  cached_network_image: ^3.3.1

  # Abrir enlaces de referencia en el navegador/app externa
  url_launcher: ^6.3.0
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
